'use client';import { Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis } from 'recharts';export function ProgressChart({data,dataKey,title}:{data:any[];dataKey:string;title:string}){return <div className="fit-card p-5"><h3 className="text-lg font-black">{title}</h3><div className="mt-4 h-64"><ResponsiveContainer width="100%" height="100%"><LineChart data={data}><XAxis dataKey="createdAt" hide/><YAxis/><Tooltip/><Line type="monotone" dataKey={dataKey} strokeWidth={3} dot={false}/></LineChart></ResponsiveContainer></div></div>}
