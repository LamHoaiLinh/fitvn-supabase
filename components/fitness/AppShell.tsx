'use client';import Link from 'next/link';import { useRouter } from 'next/navigation';import { Dumbbell } from 'lucide-react';const nav=[['Tổng quan','/dashboard'],['Hồ sơ','/profile'],['Đánh giá','/assessment'],['Lịch tập','/workout-plan'],['Tập luyện','/workout-mode'],['Tiến độ','/progress'],['Dinh dưỡng','/nutrition']];
export function AppShell({children}:{children:React.ReactNode}){const router=useRouter();async function logout(){await fetch('/api/auth/logout',{method:'POST'});router.push('/login')}return <div className="min-h-screen bg-slate-50 pb-24"><header className="sticky top-0 z-30 border-b bg-white/90 backdrop-blur"><div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-3"><Link href="/dashboard" className="flex items-center gap-2 font-black"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-gradient-to-br from-fitBlue to-fitGreen text-white"><Dumbbell size={22}/></span>FitVN</Link><button onClick={logout} className="text-sm font-bold text-slate-600">Đăng xuất</button></div></header><main className="mx-auto max-w-6xl px-4 py-6">{children}</main><nav className="fixed inset-x-0 bottom-0 z-40 border-t bg-white/95 p-2 shadow-2xl md:hidden"><div className="grid grid-cols-4 gap-1 text-center text-[11px] font-bold">{nav.slice(0,4).map(([l,h])=><Link className="rounded-xl px-1 py-2" key={h} href={h}>{l}</Link>)}</div></nav><aside className="hidden md:fixed md:bottom-8 md:left-1/2 md:z-40 md:flex md:-translate-x-1/2 md:gap-2 md:rounded-3xl md:bg-white/90 md:p-2 md:shadow-soft">{nav.map(([l,h])=><Link className="rounded-2xl px-4 py-2 text-sm font-bold hover:bg-slate-50" key={h} href={h}>{l}</Link>)}</aside></div>}
