'use client';import { useEffect } from 'react';import { flushPendingSync,getPendingQueue } from '@/lib/syncQueue';import { useCenterModal } from '@/components/notifications/CenterModal';
export function PwaSyncBoot(){const{showCenterModal}=useCenterModal();useEffect(()=>{if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>undefined);const run=async()=>{if(getPendingQueue().length)showCenterModal({message:'Một số dữ liệu đang chờ đồng bộ.',tone:'info'});const r=await flushPendingSync();if(r.synced)showCenterModal({message:'Đã đồng bộ dữ liệu tạm.'})};run();window.addEventListener('online',run);return()=>window.removeEventListener('online',run)},[showCenterModal]);return null}
