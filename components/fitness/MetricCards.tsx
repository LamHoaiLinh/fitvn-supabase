import type { Metrics } from '@/types/fitness';export function MetricCards({metrics}:{metrics?:Metrics|null}){const items=[['BMI',metrics?.bmi?`${metrics.bmi} - ${metrics.bmiLabel}`:'Chưa đủ dữ liệu'],['BMR',metrics?.bmr?`${metrics.bmr} kcal/ngày`:'Chưa đủ dữ liệu'],['TDEE',metrics?.tdee?`${metrics.tdee} kcal/ngày`:'Chưa đủ dữ liệu'],['Calo mục tiêu',metrics?.targetCalories?`${metrics.targetCalories} kcal/ngày`:'Chưa đủ dữ liệu'],['% mỡ',metrics?.bodyFatPercent?`${metrics.bodyFatPercent}%`:'Chưa đủ dữ liệu'],['Macro',metrics?.macros?`${metrics.macros.proteinG}g protein / ${metrics.macros.fatG}g fat / ${metrics.macros.carbG}g carb`:'Chưa đủ dữ liệu']];return <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{items.map(([k,v])=><div key={k} className="fit-card p-5"><div className="text-sm font-bold text-slate-500">{k}</div><div className="mt-2 text-xl font-black">{v}</div></div>)}</div>}
