import { requireAdminSession } from '@/lib/adminAuth';import { getAdminUserDetail } from '@/lib/dataRepository';import { fail,ok } from '@/lib/apiResponse';export const runtime='nodejs';export async function GET(_:Request,{params}:{params:Promise<{userId:string}>}){try{await requireAdminSession();const {userId}=await params;return ok(await getAdminUserDetail(userId))}catch(e){return fail(e)}}
