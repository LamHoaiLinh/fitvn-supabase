import { requireUserSession } from '@/lib/auth';import { getMeasurements } from '@/lib/repositories/postgresRepository';import { isDbBackend } from '@/lib/env';import { readObjects } from '@/lib/googleSheets';import { getUserById } from '@/lib/dataRepository';import { USER_HEADERS,userSheetName } from '@/lib/userSheetManager';import { fail,ok } from '@/lib/apiResponse';export const runtime='nodejs';export async function GET(){try{const s=await requireUserSession();const u=await getUserById(s.userId);if(!u)throw new Error('Không tìm thấy tài khoản.');if(isDbBackend)return ok({measurements:await getMeasurements(u.userId)});return ok({measurements:await readObjects(userSheetName(u.sheetPrefix,'Measurements'),USER_HEADERS.Measurements)})}catch(e){return fail(e)}}
