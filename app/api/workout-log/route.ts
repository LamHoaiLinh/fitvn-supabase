import { requireUserSession } from '@/lib/auth';import { workoutLogSchema } from '@/lib/validators';import { getUserById, getWorkoutLogs, saveWorkoutLog } from '@/lib/dataRepository';import { fail,ok } from '@/lib/apiResponse';export const runtime='nodejs';export async function GET(){try{const s=await requireUserSession(),u=await getUserById(s.userId);if(!u)throw new Error('Không tìm thấy tài khoản.');return ok({logs:await getWorkoutLogs(u.userId)})}catch(e){return fail(e)}}export async function POST(req:Request){try{const s=await requireUserSession(),u=await getUserById(s.userId);if(!u)throw new Error('Không tìm thấy tài khoản.');const i=workoutLogSchema.parse(await req.json());await saveWorkoutLog(u.userId,i);return ok({message:'Đã lưu workout log.'})}catch(e){return fail(e)}}
