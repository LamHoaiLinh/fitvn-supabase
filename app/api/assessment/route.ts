import { requireUserSession } from '@/lib/auth';import { assessmentSchema } from '@/lib/validators';import { classifyFitnessLevel } from '@/lib/workoutEngine';import { getCurrentProfile, getUserById, saveAssessment } from '@/lib/dataRepository';import { fail,ok } from '@/lib/apiResponse';export const runtime='nodejs';export async function POST(req:Request){try{const s=await requireUserSession(),u=await getUserById(s.userId);if(!u)throw new Error('Không tìm thấy tài khoản.');const a=assessmentSchema.parse(await req.json());const {profile}=await getCurrentProfile(u.userId);const level=classifyFitnessLevel(profile||{},a);await saveAssessment(u.userId,a as Record<string,number>,level);return ok({message:'Đã lưu đánh giá khả năng tập.',level})}catch(e){return fail(e)}}
