'use client';import Link from 'next/link';import { useRouter } from 'next/navigation';import { useState } from 'react';import { useCenterModal } from '@/components/notifications/CenterModal';
export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState('');const r=useRouter();const{showCenterModal}=useCenterModal();async function submit(e:React.FormEvent){e.preventDefault();const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await res.json();if(!res.ok)return showCenterModal({message:d.message||'Không đăng nhập được.',tone:'error'});showCenterModal({message:'Đã đăng nhập.'});const next=new URLSearchParams(window.location.search).get('next')||'/dashboard';r.push(next)}return <main className="grid min-h-screen place-items-center bg-slate-50 p-5"><form onSubmit={submit} className="fit-card w-full max-w-md p-6"><h1 className="text-3xl font-black">Đăng nhập</h1><label className="fit-label mt-5 block">Email<input className="fit-input mt-1" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="fit-label mt-4 block">Mật khẩu<input className="fit-input mt-1" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="fit-btn-primary mt-6 w-full">Đăng nhập</button><div className="mt-4 flex justify-between text-sm font-bold text-fitBlue"><Link href="/register">Tạo tài khoản</Link><Link href="/forgot-password">Quên mật khẩu</Link></div></form></main>}
