'use client';import { useState } from 'react';import Link from 'next/link';import { useCenterModal } from '@/components/notifications/CenterModal';export default function Forgot(){const[email,setEmail]=useState(''),[token,setToken]=useState('');const{showCenterModal}=useCenterModal();async function submit(e:React.FormEvent){e.preventDefault();const res=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await res.json();if(!res.ok)return showCenterModal({message:d.message,tone:'error'});setToken(d.resetToken||'');showCenterModal({message:'Đã tạo mã khôi phục.'})}return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="fit-card w-full max-w-md p-6"><h1 className="text-3xl font-black">Quên mật khẩu</h1><p className="mt-2 text-sm text-slate-600">MVP chưa tích hợp email. Ở development, app trả mã để admin hỗ trợ gia đình.</p><input className="fit-input mt-5" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><button className="fit-btn-primary mt-4 w-full">Tạo mã</button>{token&&<div className="mt-4 rounded-2xl bg-slate-100 p-3 text-sm font-bold">Mã: {token}</div>}<Link className="mt-4 block text-center font-bold text-fitBlue" href="/reset-password">Nhập mã khôi phục</Link></form></main>}
