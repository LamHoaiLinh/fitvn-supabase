'use client';import { useState } from 'react';import { useRouter } from 'next/navigation';import { useCenterModal } from '@/components/notifications/CenterModal';export default function Reset(){const[form,setForm]=useState({email:'',token:'',password:''});const r=useRouter();const{showCenterModal}=useCenterModal();async function submit(e:React.FormEvent){e.preventDefault();const res=await fetch('/api/auth/reset-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await res.json();if(!res.ok)return showCenterModal({message:d.message,tone:'error'});showCenterModal({message:'Đã đổi mật khẩu.'});r.push('/login')}return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="fit-card w-full max-w-md p-6"><h1 className="text-3xl font-black">Đặt lại mật khẩu</h1>{[['email','Email'],['token','Mã khôi phục'],['password','Mật khẩu mới']].map(([k,l])=><label key={k} className="fit-label mt-4 block">{l}<input className="fit-input mt-1" type={k==='password'?'password':'text'} value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<button className="fit-btn-primary mt-6 w-full">Đổi mật khẩu</button></form></main>}
