'use client';import Link from 'next/link';import { useRouter } from 'next/navigation';import { useState } from 'react';import { useCenterModal } from '@/components/notifications/CenterModal';
export default function Register(){const[form,setForm]=useState({name:'',email:'',password:''});const r=useRouter();const{showCenterModal}=useCenterModal();async function submit(e:React.FormEvent){e.preventDefault();const res=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await res.json();if(!res.ok)return showCenterModal({message:d.message,tone:'error'});showCenterModal({message:'Đã tạo tài khoản.'});r.push('/dashboard')}return <main className="grid min-h-screen place-items-center bg-slate-50 p-5"><form onSubmit={submit} className="fit-card w-full max-w-md p-6"><h1 className="text-3xl font-black">Tạo tài khoản</h1>{[['name','Tên của bạn'],['email','Email'],['password','Mật khẩu']].map(([k,l])=><label key={k} className="fit-label mt-4 block">{l}<input className="fit-input mt-1" type={k==='password'?'password':'text'} value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<button className="fit-btn-primary mt-6 w-full">Đăng ký</button><Link className="mt-4 block text-center font-bold text-fitBlue" href="/login">Đã có tài khoản</Link></form></main>}
