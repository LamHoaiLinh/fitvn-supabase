'use client';import { useState } from 'react';import { useRouter } from 'next/navigation';import { useCenterModal } from '@/components/notifications/CenterModal';export default function AdminLogin(){const[password,setPassword]=useState('');const r=useRouter();const{showCenterModal}=useCenterModal();async function submit(e:React.FormEvent){e.preventDefault();const res=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});const d=await res.json();if(!res.ok)return showCenterModal({message:d.message,tone:'error'});showCenterModal({message:'Đã đăng nhập admin.'});r.push('/admin/dashboard')}return <main className="grid min-h-screen place-items-center bg-slate-900 p-5"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-6"><h1 className="text-3xl font-black">Admin FitVN</h1><input className="fit-input mt-5" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Mật khẩu admin"/><button className="fit-btn-primary mt-5 w-full">Đăng nhập admin</button></form></main>}
